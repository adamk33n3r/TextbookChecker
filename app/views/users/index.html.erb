<h1>Listing users</h1>

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th>Year</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= user.year %></td>
        <td><%= div_to 'Show', user, :class => "small_link" %></td>
        <td><%= div_to 'Edit', edit_user_path(user), :class => "small_link" %></td>
        <td><%= div_to 'Destroy', user, method: :delete, :class => "small_link", data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= div_to 'New User', new_user_path, :class => "small_link" %>
