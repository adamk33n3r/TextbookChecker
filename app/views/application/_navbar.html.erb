<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "TextbookChecker", root_path, class: "navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= navbar_link "Home", root_path %>
        <% if logged_in? %>
          <%= navbar_link "Students", students_path %>
          <%= navbar_link "Textbooks", textbooks_path %>
          <%= navbar_link "Professors", professors_path %>
          <%= navbar_link "Courses", courses_path %>
        <% else %>
          <li <% if current_page? signup_path %><%= 'class=active'%><% end %>><%= link_to "Sign Up", signup_path %></li>
          <li <% if current_page? login_path %><%= 'class=active'%><% end %>><%= link_to "Log In", login_path %></li>        
        <% end %>
      </ul>
      <% if logged_in? %>
        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.username.titleize %><div class="caret"></div></a>
            <ul class="dropdown-menu">
              <%= navbar_link "Profile", current_user %>
              <li><%= link_to "Log Out", logout_path, :method => :delete %></li>
            </ul>
          </li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>