<%= bootstrap_form_for [@ta, @rating] do |f| %>
  <%= f.radio_button_group :vote_id, Vote, label: "Vote" %>
  <%# f.collection_select(:vote_id, Vote.all, :id, :method) %>
  <%= f.text_area :content, label: "Review", size: "30x8" %>
  <div class="button-group">
    <%= blink_to "Cancel", :back %>
    <%= f.submit class: "btn-primary" %>
  </div>
<% end %>